"use client";

/**
 * OnboardingForm foi removido — a tela de onboarding nao existe mais.
 * Este componente ficou como stub para evitar quebrar imports orfaos.
 */
export function OnboardingForm() {
  return null;
}
